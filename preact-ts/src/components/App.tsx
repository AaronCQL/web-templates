import { h, FunctionalComponent } from "preact";

const App: FunctionalComponent = () => {
  return (
    <h1 style={{ fontWeight: 400, textAlign: "center" }}>
      Hello from <code>App.tsx</code>
    </h1>
  );
};

export default App;
